@{
    ViewData["Title"] = "Senha redefinida";
}

<section class="auth-shell">
    <div class="auth-card">
        <span class="eyebrow">Seguranca de acesso</span>
        <h1>Senha redefinida</h1>
        <p class="hero-description">Se o token era valido, a nova senha ja pode ser usada no proximo login.</p>
        <a asp-action="Login" class="btn btn-primary">Ir para login</a>
    </div>
</section>
